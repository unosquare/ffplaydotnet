<Window x:Name="window" x:Class="Unosquare.FFplayDotNet.Sample.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Unosquare.FFplayDotNet.Sample"
        xmlns:ffme="clr-namespace:Unosquare.FFplayDotNet;assembly=Unosquare.FFplayDotNet"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition Height="50"></RowDefinition>
        </Grid.RowDefinitions>
        <ffme:MediaElement Name="Media"></ffme:MediaElement>
        <Button Grid.Row="1" Margin="20,6" Command="{Binding ElementName=window, Mode=OneWay, Path=OpenCommand}">Test</Button>
    </Grid>
</Window>
